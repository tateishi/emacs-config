# emacs-config
emacs config with use-package

## 初期化

### 準備

eask を使うので、関連ツールをインストール。

#### node をインストール

- nvm を使う場合
```bash
nvm install 24
nvm use 24
```

#### Eask をインストール

```bash
npm install -g @emacs-eask/cli
```

### packageのインストール

```bash
cd ~/.config/emacs/
eask install
```
